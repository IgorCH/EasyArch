angular
  .module('theme.core.rw_resPopup_controller', ['theme.core.services'])
  .controller('RWResPopupController', ['$scope', '$theme', '$bootbox', '$modal', 'ReportWriterDataSource',
    function($scope, $theme, $bootbox, $modal, ReportWriterDataSource) {

      $scope.init = function () {
        debugger;

      };


    }])
;
